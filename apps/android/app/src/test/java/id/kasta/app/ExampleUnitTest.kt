package id.kasta.app

import org.junit.Assert.assertEquals
import org.junit.Test

class ExampleUnitTest {
    @Test
    fun applicationIdRemainsStable() {
        assertEquals("id.kasta.app", BuildConfig.APPLICATION_ID)
    }
}
