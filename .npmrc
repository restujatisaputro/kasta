# Ensure lifecycle scripts can find the exact Node executable that launched pnpm, including bundled
# developer runtimes where node.exe is not installed globally.
scripts-prepend-node-path=true

# Workspace packages are linked locally; dependency versions remain locked in pnpm-lock.yaml.
link-workspace-packages=true
