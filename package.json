{
  "name": "kasta-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo KASTA - Keuangan dan Asistensi UMKM",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22 <25",
    "pnpm": "11.9.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "check": "pnpm -r --if-present check",
    "dev:web": "pnpm --filter kasta_web dev",
    "format": "prettier --write package.json pnpm-workspace.yaml prettier.config.cjs tsconfig.base.json README.md CONTRIBUTING.md .github apps/web packages infrastructure documentation tests apps/android/README.md",
    "format:check": "prettier --check package.json pnpm-workspace.yaml prettier.config.cjs tsconfig.base.json README.md CONTRIBUTING.md .github apps/web packages infrastructure documentation tests apps/android/README.md",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "test:e2e": "playwright test --config tests/e2e/playwright.config.ts",
    "test:a11y": "playwright test --config tests/e2e/playwright.config.ts --grep @a11y"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "prettier": "^3.5.3",
    "prettier-plugin-svelte": "^3.4.0"
  }
}
