# @kasta/shared

Tipe dan konfigurasi nonrahasia yang dapat digunakan oleh package TypeScript KASTA. JSON di
`config/` bersifat netral bahasa dan dapat dikonsumsi generator/tooling. Aturan bisnis, model
database, token, dan secret tidak boleh ditempatkan di package ini.
